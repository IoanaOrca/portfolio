---
import { Icon } from 'astro-icon';
import ThemeToggle from '@components/ThemeToggle.astro';

const links = [
  {
    label: 'Home',
    url: '/',
  },
  {
    label: 'Crafting',
    url: '/crafting',
  },
  {
    label: 'Cooking',
    url: '/cooking',
  },
  {
    label: 'Coding',
    url: '/coding',
  },
  {
    label: 'Blog',
    url: '/blog',
  },
];
---

<nav class="navbar bg-base-200/60 sticky top-0 z-50 backdrop-blur shadow">
  <div class="navbar-start">
    <a class="btn btn-ghost btn-circle" href="/">
      <Icon name="orca" class="h-7 fill-current" />
      <span class="sr-only">IoanaOrca</span>
    </a>
  </div>
  <div class="navbar-end">
    {/* Desktop Menu */}
    <div class="flex-none hidden md:flex">
      <ul class="menu menu-horizontal px-1">
        {
          links.map((link) => (
            <li>
              <a class="font-semibold uppercase" href={link.url}>
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </div>

    <ThemeToggle />

    {/* Mobile Menu */}
    <details class="dropdown dropdown-end md:hidden">
      <summary class="btn btn-ghost">
        <Icon name="HeroiconsBars3CenterLeft" class="h-7 fill-current scale-x-[-1]" />
        <span class="sr-only">Menu</span>
      </summary>
      <ul class="menu dropdown-content mt-3 p-2 shadow bg-base-100 rounded-box">
        {
          links.map((link) => (
            <li>
              <a class="font-semibold uppercase" href={link.url}>
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </details>
  </div>
</nav>
